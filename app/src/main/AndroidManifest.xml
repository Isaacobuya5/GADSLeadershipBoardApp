<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gadsleadershipboardapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/DefaultTheme">
        <activity android:name=".SubmissionSuccesful"
            android:theme="@style/AppTheme.TransLucentTheme"></activity>
        <activity android:name=".SubmissionFailed"
            android:theme="@style/AppTheme.TransLucentTheme"/>
        <activity android:name=".ProjectSubmissionConfirmation"
            android:theme="@style/AppTheme.TransLucentTheme"/>
        <activity
            android:name=".SubmitProjectActivity"
            android:theme="@style/SubmitProjectActivityTheme" />
        <activity
            android:name=".LeaderBoardActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>